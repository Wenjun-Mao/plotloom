# 2026-09-04 M1-12A development-data reset

This local checkpoint was performed after commit `f6fab1f` and before the
canonical authoring schema changed. No Plotloom service was listening on the
configured `127.0.0.1:8775` endpoint.

The exact source-checkout data directory was moved, not deleted, to:

`/Users/wjmao/projects/HU/plotloom-data-backups/pre-m1-12a-2026-09-04/data`

The backed-up database was internally consistent, used migration head
`0007_v2_run_failure_codes`, and had SHA-256:

`45bcf78b650083d7b4d45dee1faca35b70cfa39c80050e7d0a4a403e70bd6a94`

A second SQLite export contains only `v2_text_provider_profiles` and
`v2_provider_profile_selection`:

`/Users/wjmao/projects/HU/plotloom-data-backups/pre-m1-12a-2026-09-04/provider-profiles.sqlite3`

Its SHA-256 is
`1c074b5251a62826ea3310ad07ab49c524b408a6a13c8aa37465eb821f697879`.
The export contains the `default` and `qwen36_35b` public profiles, with
`default` active. Plotloom's recursive secret-shape checks reported no secret
field or value in either exported profile.

A fresh data directory was then created and migrated through
`0009_v2_work_unit_repair_scopes`. The two public profiles and active selection
were restored exactly. After the M1-12A migration was implemented, the same
fresh database was advanced to `0010_v2_schema_approvals`; it contained zero
gate or Approval rows. Post-reset counts were:

- 2 text-provider profiles and 1 selection;
- 0 projects;
- 0 generation runs;
- 0 media tasks; and
- 0 artifacts, gate results, or Approval decisions.

The repository-root `.env` remained outside the reset scope. It was consulted
by the normal settings resolver to identify the data paths, but was not copied,
edited, or included in either backup artifact. The external backup is local
operational material and is intentionally not part of the Git repository or
release wheel.
