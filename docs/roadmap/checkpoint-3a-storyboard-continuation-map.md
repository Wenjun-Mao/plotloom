# Checkpoint 3A existing-capability and gap map

`ProjectBrief`, Story Bible, and Story Graph are canonical author-owned inputs;
the pipeline owns generated Scene Beats and Storyboard; the browser owns only
the explicit request and view state. `StageHead.status` is the currentness
authority. Existing Scene Beats and Storyboard pages already expose scenes,
beats, dialogue, audio, shots, validation focus, and normal canonical saves.
Existing Trace and Quarantine pages own progress, validation evidence,
server-issued exact repair, and stage rebuild.

The missing connection was a proposal-side explicit continuation that selected
only the missing/stale downstream canonical range. Checkpoint 3A adds that one
connection and leaves the owners, schemas, gate/Approval semantics, media
workflow, and repair policy unchanged.
