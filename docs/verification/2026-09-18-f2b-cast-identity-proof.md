# F2B cast-linked identity proof

Date: 2026-09-18. This is a completed, narrow technical proof of two cast-owned
exploratory reference studies. It does not claim human creative approval, a
Story Bible/Shot/Approval, F5 production cross-shot identity proof, or F7 voice
proof.

## Delivered seam

`1b7b463` extends the existing selected character-reference and P1.5 image-job
route with the accepted cast's explicit consumer-ID mapping. A selected
reference freezes the mapped cast revision/hash and appearance/image direction;
the image request and its frozen `characterIdentity` retain that lineage. A
reopened or source-stale cast makes the reference, dependent image job, and
same-person review inapplicable through the existing currentness checks. The
existing managed original remains untouched and reviewable.

The pinned specialist skill and the generated `COPY_ASSIGNMENT.txt` require
`characterIdentity[].acceptedCast` appearance/image direction to be preserved
as cast-owned identity facts. The frozen shot remains the owner of state,
costume, pose, composition, lighting, and camera.

The obsolete, unrendered Story-Bible-owned reference component is removed; the
accepted-cast panel is the only F2B entry point. Its session key now clears
proposal, assignment, and parent state when project/cast ownership changes, so
late reads cannot populate a different cast. Prepared and exported proposals
are explicitly cancellable; cancellation records the reason and leaves a late
delivery inapplicable rather than selecting or publishing a reference.

Focused checks on this candidate: `uv run pytest
tests/test_project_storage_cast.py tests/test_project_storage_image_delivery_contracts.py
-q` passed (11); frontend typecheck and deterministic build passed. The build
refreshed `src/plotloom/static/`; its pre-existing over-500 kB chunk warning
remains. This is automated source/runtime evidence, not live creative proof.

## Live proof at `1f375116`

The supported CUA production browser opened isolated F2A project
`4cdbe9c5-dc47-4365-98a3-7ba022433556` at `http://127.0.0.1:8776/v2/` with its
project-local roots. It showed accepted cast r2, `C01 -> C01` and `C02 -> C02`,
the accepted source/outline/section map/graph, and no Story Bible, scene beats,
storyboard, or pre-existing managed assets. That absence is expected for F2B:
the cast-only panel was enabled and states it creates no Story Bible, Shot,
Approval, or production cross-shot claim. The old `7b62bc3` viewport is
**historical pre-cast UI evidence**, not current F2B evidence.

The UI prepared and copied C01 (`林澈`) as an original study, pinned the
specialist before ImageGen, inspected and refreshed the generated raster, then
explicitly selected it as r1 (`Terra technical operator`). The UI then froze it
as the refinement parent, pinned and generated the distinct refinement, and
selected it as r2. The browser was reloaded and the project normally closed and
reopened; r2 persisted and r1 was visibly superseded. No canonical stages were
fabricated. CUA screen captures showed this live state; the PNGs below are the
durable visual artifacts.

| Study | Job / ImageGen task | Output SHA-256 |
| --- | --- | --- |
| Original | `ij_dfa724a53d3040a2a330092909ea2600` / `exec-ca24a53e-74f4-4b3a-b70f-120e923aa826` | `e835e874525906688a4abf63e6e547f1f786d2162dc7e85c8b858df230c2af96` |
| Refinement | `ij_05100883240447b182e163b2e25dbf47` / `exec-db9ba9a2-5586-4d50-9393-3264809e2cf6` | `1fb4700c73834b81c2ea3f64d6754cc0b24ba1bfcb472d8993161d2d7a2f2e0d` |

Their package directories are respectively under
`.local/relay/fa9d3aa6-9039-4c02-ab16-e30e0a380943/live-proof/outputs/20260918T023736393855Z__ij_dfa724a53d3040a2a330092909ea2600/`
and
`.local/relay/fa9d3aa6-9039-4c02-ab16-e30e0a380943/live-proof/outputs/20260918T024024706909Z__ij_05100883240447b182e163b2e25dbf47/`.
After native admission, the project also retained the exact managed originals at
`assets/e8/e835e874525906688a4abf63e6e547f1f786d2162dc7e85c8b858df230c2af96`
and
`assets/1f/1fb4700c73834b81c2ea3f64d6754cc0b24ba1bfcb472d8993161d2d7a2f2e0d`
under its project-local output root.
Both completions record `codex_imagegen`, available true, actual prompts and
task IDs, plus code revision `1f375116aa4cb862b294830aa3398c9fc6d0a517`,
`plotloom-image-specialist.v3`, skill hash
`f9b01a569628878dea3c3267c372d4a9bac131f65dcc1567e610feb460419c30`, and
actual coordinator provenance (`gpt-5.6-terra`, high). The refinement's frozen
`parent_output` SHA-256 exactly matches the original output.

Cleanup was invoked once per durable delivery. It did not remove staging: the
original helper refused the non-private task directory; the refinement helper
could not resolve its package path. Both staged PNGs remain at
`/Users/wjmao/.codex/generated_images/01a0b25e-aedd-78c0-b5d5-02304e33c7f0/`.
The refinement's first call failed before generation because the tool could not
resolve the project-local parent path. Its succeeding call used the retained
default-staging original with the exact frozen parent SHA-256; no substitute or
duplicate generation was used.

## Attended independent review and technical browser regression

An independent Terra high-reasoning read-only review inspected both rasters and
all package/pin/completion evidence. It found strong same-person continuity in
facial structure, wet dark hair, tired-alert expression, and navy jacket, plus a
materially distinct seated-desk/window versus standing-doorway composition. No
obvious malformed anatomy, text, watermark, extra people, or copied composition
was found. The retained review is
`.local/relay/fa9d3aa6-9039-4c02-ab16-e30e0a380943/live-proof/f2b-independent-terra-review.md`.
It records the material limit: actual output is photorealistic rather than the
requested semi-realistic painterly direction. This is not face recognition or
product acceptance.

Separately, technical browser evidence at `1f37511` passed three production E2E
journeys, two focused backend tests, 159 frontend tests, types, and deterministic
build. Its retained summary is `.local/relay/f2b-browser-proof/test-log.md`, not
a full raw log. It is distinct from, and does not replace, this fresh live proof.

## Remaining seam

F2B's bounded technical identity-study proof is complete. F3 is the next
creative-stage seam. F5/F7 production cross-shot and voice proof remain separate.
