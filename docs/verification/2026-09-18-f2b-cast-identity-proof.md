# F2B cast-linked identity proof — superseded blocked-pilot receipt

Date: 2026-09-18. This is a retained, incomplete technical receipt. It does
not claim F2B acceptance, generated media, human creative approval, or voice
proof.

## Delivered seam

`fae7803` extends the existing selected character-reference and P1.5 image-job
route with the accepted cast's explicit consumer-ID mapping. A selected
reference freezes the mapped cast revision/hash and appearance/image direction;
the image request and its frozen `characterIdentity` retain that lineage. A
reopened or source-stale cast makes the reference, dependent image job, and
same-person review inapplicable through the existing currentness checks. The
existing managed original remains untouched and reviewable.

The pinned specialist skill and the generated `COPY_ASSIGNMENT.txt` now require
`characterIdentity[].acceptedCast` appearance/image direction to be preserved
as cast-owned identity facts. The frozen shot remains the owner of state,
costume, pose, composition, lighting, and camera.

Focused checks: `uv run pytest tests/test_project_storage_cast.py
tests/test_project_storage_image_identity_contracts.py
tests/test_image_job_exchange_contracts.py -q` passed (3); frontend typecheck
and deterministic build passed; both modified specialist skills passed their
skill validation. An attended independent read-only review passed after the
frozen-instruction correction.

## Superseded prerequisite diagnosis

The copy of the F2A proof project `4cdbe9c5-dc47-4365-98a3-7ba022433556` was
opened in the production workbench at `http://127.0.0.1:8776/v2/`. It retains
accepted cast r2 with `C01 -> C01` and `C02 -> C02`, but has only a ready story
graph. Its Story Bible, scene beats, and storyboard are missing; it has zero
managed assets, character-reference decisions, and image jobs. The browser
shows `角色 · 0` and the disabled reference controls; retained viewport evidence
is `.playwright-cli/page-2026-09-18T01-29-53-514Z.png`.

This correctly described the old implementation's limitation, but its proposed
remedy was wrong for F2B: the old proposal path hard-required Story Bible even
though exploratory reference studies need neither a Bible, Shot nor Approval.
The F2B replacement resolves the current accepted mapped cast subject directly;
therefore no canonical stages may be fabricated to mask this old API contract.
No ImageGen call was made and no media placeholder was substituted.

## Next approved seam

Use the disposable F2A project with its accepted mapped cast to prepare an
exploratory cast-owned proposal, deliver/inspect/select a reference, and make a
second distinct refinement study from that parent candidate. The remaining F5/F7
cross-shot obligation remains separate and must not be claimed here.
