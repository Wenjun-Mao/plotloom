# F2B cast-linked identity proof — current blocked-pilot receipt

Date: 2026-09-18. Updated after the `1b7b463` candidate cleanup. This is a
retained, incomplete technical receipt. It does
not claim F2B acceptance, generated media, human creative approval, or voice
proof.

## Delivered seam

`1b7b463` extends the existing selected character-reference and P1.5 image-job
route with the accepted cast's explicit consumer-ID mapping. A selected
reference freezes the mapped cast revision/hash and appearance/image direction;
the image request and its frozen `characterIdentity` retain that lineage. A
reopened or source-stale cast makes the reference, dependent image job, and
same-person review inapplicable through the existing currentness checks. The
existing managed original remains untouched and reviewable.

The pinned specialist skill and the generated `COPY_ASSIGNMENT.txt` require
`characterIdentity[].acceptedCast` appearance/image direction to be preserved
as cast-owned identity facts. The frozen shot remains the owner of state,
costume, pose, composition, lighting, and camera.

The obsolete, unrendered Story-Bible-owned reference component is removed; the
accepted-cast panel is the only F2B entry point. Its session key now clears
proposal, assignment, and parent state when project/cast ownership changes, so
late reads cannot populate a different cast. Prepared and exported proposals
are explicitly cancellable; cancellation records the reason and leaves a late
delivery inapplicable rather than selecting or publishing a reference.

Focused checks on this candidate: `uv run pytest
tests/test_project_storage_cast.py tests/test_project_storage_image_delivery_contracts.py
-q` passed (11); frontend typecheck and deterministic build passed. The build
refreshed `src/plotloom/static/`; its pre-existing over-500 kB chunk warning
remains. This is automated source/runtime evidence, not live creative proof.

## Live-proof blocker

The copy of the F2A proof project `4cdbe9c5-dc47-4365-98a3-7ba022433556` was
opened in the production workbench at `http://127.0.0.1:8776/v2/`. It retains
accepted cast r2 with `C01 -> C01` and `C02 -> C02`, but has only a ready story
graph. Its Story Bible, scene beats, and storyboard are missing; it has zero
managed assets, character-reference decisions, and image jobs. The browser
shows `角色 · 0` and the disabled reference controls; retained viewport evidence
is `.playwright-cli/page-2026-09-18T01-29-53-514Z.png`.

The former Story-Bible prerequisite was removed by `1b7b463`: exploratory cast
studies now resolve the current accepted mapped cast subject directly. No
canonical stages may be fabricated to mask the old contract.

The independently observed native desktop remained locked for this continuation.
Built-in ImageGen is an available task capability, but that does not create the
required attended production-browser review surface or authorize substituting
an API/scripted fixture for it. No ImageGen call was made, no media placeholder
was substituted, and no browser review/reopen is claimed.

## Next approved seam

When the supported desktop surface is usable, use the disposable F2A project
with its accepted mapped cast: prepare/copy a frozen original study, pin the
specialist, generate and inspect it, refresh/select it, then make and review a
distinct refinement study from that parent. The remaining F5/F7 cross-shot
obligation remains separate and must not be claimed here.
