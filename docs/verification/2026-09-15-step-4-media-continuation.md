# Step 4 media continuation — identity selected; exact-profile adaptation blocked

Captured 2026-09-15 from one attended continuation of the retained fresh
current-contract project. This receipt is a partial operational record, not
Step 4 completion, storyboard approval, human/product acceptance, audiovisual
acceptance, Alpha qualification, or release evidence.

## Scope and preserved lineage

- Continued only project `fef96fc8-86f8-4132-91bd-32d58f5bb504`; no text
  generation, new project, provider/profile/configuration, or application
  source change was made.
- The older retained lineage `ae4595cf-2bcc-480a-839b-af3a8901240c` was not
  opened or altered.
- The existing exploratory character-reference proposal
  `ij_5cd19d01283246b4b3dc501b048eadc2` was inspected before work: its frozen
  request was complete and current, had no input references (therefore none to
  view), and had neither an executor pin nor a delivery. The earlier failed
  preflight did not leave an incomplete pin or a duplicate job.

## First real identity-reference delivery

- The package's first specialist preflight passed at committed source revision
  `d893a6489980b621d9c3aab6bdf5da12e3568342`, writing its executor pin with
  skill hash `714b864e05ca5d0faea8bd5bf0d5c99459aa88f7503d5feba9d86e6f9e5264c7`.
- One built-in ImageGen call produced and received visual inspection of
  `delivery/outputs/exec-e5f0ad04-a8b7-4a62-9016-ff00d695fe61.png`
  (`52fbc6cfde52413a0971123e2bd7c66974b44bda5dd6badf21edea41d4765b17`). It
  visibly depicts one young Chinese man with a calm expression, tousled black
  hair, olive-gray jacket, dark knit, and neutral indoor background; no station,
  watch, action, text, or additional person was observed.
- The complete hash-bound manifest is
  `delivery/completion.json`. It records normalized tool evidence
  `codex_imagegen`, actual task id
  `e5f0ad04-a8b7-4a62-9016-ff00d695fe61`, the exact submitted prompt, and the
  matching request/job IDs. The identity proposal remains exploratory: no
  storyboard, reference, or keyframe approval/selection is claimed.

## Original identity staging resolution

The completed identity delivery was not regenerated, re-pinned, or rewritten.
Its original worker verified only its exact returned task staging directory with
`lstat`, current-UID, non-symlink, and no-cooperating-writer checks, changed
that directory non-recursively to `0700`, and ran the existing cleanup helper
once. The director subsequently verified that the staged source is absent and
the durable output still has SHA-256
`52fbc6cfde52413a0971123e2bd7c66974b44bda5dd6badf21edea41d4765b17`.
This resolves the private-staging boundary for that completed delivery only; it
does not relax cleanup for any other task directory.

## Continued authoring and first still

- An owned local Plotloom runtime opened only the retained project and the
  normal workbench **刷新服务器版本** action revalidated its server state.
- The original delivery was refreshed through the author workflow, visually
  reviewed, and selected as Lin Che's primary identity reference, revision 1.
  The stored note attributes that review to Codex and explicitly does not claim
  human or product acceptance.
- The first adjacent opening shot prepared and exported P1 job
  `ij_c961d30b315944faa273384545555055`. Its pin passed at committed revision
  `a186d4280333413e7e49dfd8c4ff9e5684253dcf`; its package-bound identity
  reference was viewed. One built-in ImageGen call produced visually inspected
  source still `exec-64d40b1a-a4b0-482a-a4ae-135e765c5e5c.png`, hash
  `7789a5fbcaaf2aec2bebc9740c95e41dd20e42486e53539db54ef15757c7a8f5`.
  The manifest was hash-valid, the staging directory passed the same exact
  private-directory checks, and the existing helper removed its staged source
  after validating the durable project-local delivery.
- The workbench accepted the delivery as candidate `18cc2a22`, retained it,
  stored visual intent r1, selected it for the opening shot, and recorded a
  Codex-attributed same-person/shot-state review. The visual review found one
  identity-consistent Lin Che, alone in the deep-night station with the bench
  present and no watch or additional person. This is not face recognition or
  human/product acceptance.

## Exact-profile blocker

The accepted source still is `941×1672`, so the workbench correctly rejected
it for the frozen H3 Portrait Fast `576×1024` ratio rather than cropping or
padding it. It prepared a dedicated `keyframe_adaptation` job
`ij_c6f81e5fd28441d7abf2a34e129221d9`, whose package freezes the selected
source, identity reference, and exact `576×1024` output contract.

Its pin passed and the relevant source and identity references were viewed. A
single actual ImageGen adaptation call returned
`exec-4bd4247c-c50b-4ff7-83d0-6b7122a752cf.png`, SHA-256
`75897b477f040e58cc03728d8b670347b22e49248b8338108a77cbd898742d79`, but
the bytes are again `941×1672`. The file is preserved in this task's verified
private staging directory. No completion manifest, cleanup, import, selection,
H3 freeze/dispatch, or retry was performed for that incompatible output.

This is a provider output-contract failure, not a condition that the author
workflow may hide with resizing, cropping, or black padding. Downstream work
was stopped: the second adjoining keyframe, both H3 clips, audiovisual review,
sequential playback, snapshot, close/reopen, and isolated restore are all
unassessed.

## Prior stop condition (superseded for the completed identity delivery)

Before the resolution above, the required exact-staging cleanup was attempted
once and refused:

```text
cleanup refused: staging root must be private to the current user
```

The returned staging directory had mode `drwxr-xr-x`; the helper requires a
private user directory. The exact returned staging file remains preserved at
`/Users/wjmao/.codex/generated_images/01a0a6f6-53a8-7a83-8d53-8cdb175abd42/exec-e5f0ad04-a8b7-4a62-9016-ff00d695fe61.png`.
Its durable package copy and hash are intact. No permission workaround,
cleanup retry, package overwrite, refresh/import, creative selection,
keyframe generation, H3 preparation/dispatch, playback, close/reopen,
snapshot, or isolated restore was attempted.

## Counts and disposition

- Built-in ImageGen: **3** of at most 4: the completed identity delivery, one
  accepted original-shot still, and one incompatible exact-profile adaptation.
  A local unreadable-input rejection occurred before a provider invocation and
  is not counted as a generation.
- New H3 I2V jobs: **0** of at most 2.
- Genuine audiovisual review: unavailable because no clip was produced; no
  audio stream or contact-sheet evidence is being substituted for it.
- The owned local runtime must be stopped after recording this result.

Step 4 remains partial. A later, separately authorized continuation must retain
the incompatible adaptation evidence and reassess the provider's ability to
honor the exact frozen geometry before attempting any new image request. It
must not replay the same delivery or dispatch H3 from the mismatched still.

## Gateway-crop continuation — two clips ingested, audiovisual review unavailable

Captured 2026-09-15 from the approved continuation at source revision
`6fd7589da181def0b7af888cea03481dc1503e60`. This section supersedes only the
previous no-dispatch/count disposition. It remains a secret-free operational
receipt, not Step 4 completion, human/product acceptance, audiovisual
acceptance, Alpha qualification, or release evidence.

The retained project `fef96fc8-86f8-4132-91bd-32d58f5bb504` alone was opened
through the normal project-folder runtime. The old project
`ae4595cf-2bcc-480a-839b-af3a8901240c` was not opened or changed; no text was
regenerated; no source, runtime configuration, deployment, or provider setting
was changed. The H3 backend was live with the reviewed Portrait Fast profile
(`576×1024`, five seconds, native audio, adapter version 4).

### First clip: retained reviewed opening and explicit crop

- The original opening asset `18cc2a22-cd73-44e5-b132-83599ac04508` remained
  byte-identical at SHA-256
  `7789a5fbcaaf2aec2bebc9740c95e41dd20e42486e53539db54ef15757c7a8f5`.
- The normal video-prepare route froze current reviewed binding
  `7050bb33-4c65-4cc1-8b54-77b6e2f7d474`, the selected identity decision and
  its prior same-person review, the original source hash, and explicit
  `allowCenterCrop: true` / `cover_center_crop` under the Portrait Fast profile.
  The gateway, not Plotloom, owns that centered crop; no local derivative or
  replacement ImageGen sizing request was created.
- One submit only created Plotloom job `vj_86676092a8e84516922f650838c752e0`
  and gateway job `h3_f8bd16540b394e0a8303615f5d7a1265`. Reconciliation ingested
  output SHA-256 `1e95ed45f20f6287f6686a246f1e0164db6dd549d87472e30b1437cbd6302dc6`.
  The trusted probe observed `5.167` seconds, `576×1024`, H.264, AAC, 24 fps,
  and 124 frames. The candidate remains unselected.

### Adjoining still and second clip

- The single remaining ImageGen allowance produced original job
  `ij_3ab79dc6b5244d8eae4567a979a52115`. Its preflight pin records the committed
  revision above and the specialist hash
  `714b864e05ca5d0faea8bd5bf0d5c99459aa88f7503d5feba9d86e6f9e5264c7`.
  Its only frozen `character_identity` reference was viewed at SHA-256
  `52fbc6cfde52413a0971123e2bd7c66974b44bda5dd6badf21edea41d4765b17`.
- Built-in ImageGen task `2437d63a-7fb7-4840-bbf8-019020c3a1b6` delivered
  `exec-2437d63a-7fb7-4840-bbf8-019020c3a1b6.png`, SHA-256
  `ea5ac75987f3536f3c8c2351bbfb902fd35408a5250c4aab24d35023f350a8aa`, as
  managed asset `dcbf21f1-369d-4b96-ab89-6f9f6a49cff0` (`941×1672`). Its complete
  receipt was accepted through normal Refresh delivery. The exact returned
  staging directory and file were current-UID, owned, regular and non-symlink,
  with no open cooperating writer; only that `0755` task directory was changed
  non-recursively to `0700`. The cleanup helper then validated the durable
  output and removed only that exact staged source.
- A Codex-attended visual review recorded the selected identity and adjoining
  composition: one consistent Lin Che sits at the same night-station bench,
  looking toward an old silver pocket watch still beneath it; no pickup, extra
  person, or text was observed. The resulting reviewed binding is
  `0ea08806-875a-40e1-9607-2b3b77628228`, with same-person review
  `ba10dcb0-8293-4c42-8b9c-c100b28714b0`. This is neither face recognition nor
  human/product acceptance.
- The new `941×1672` still differs from `576×1024` by a centered crop of at
  most one source pixel in width. The reviewed, non-material near-9:16 crop was
  made explicit in the frozen second request; it was not hidden as resizing or
  a local derivative. One submit only created Plotloom job
  `vj_fffbcebbca8945fc99dbae42863f70b2` and gateway job
  `h3_f9e84bff8b6c44c691e38b3ed91aa6b2`. Reconciliation ingested output SHA-256
  `45079df8f791cc3d32440b507a3c5d063bc52caf98ae750be216c22435b8854d`, with the
  same trusted `5.167` second, `576×1024`, H.264/AAC, 24-fps/124-frame result.
  The candidate remains unselected.

### Stop condition and counts

Both ingested candidates were left unselected. An attended workbench playback
attempt made the in-app browser renderer crash as video started, and no other
browser automation surface was available. Consequently there was no genuine
audiovisual review of either clip: no claim is made about audible ambience,
dialogue, intelligibility, lip sync, performance, or motion continuity. Codec
metadata and still inspection are not substitutes. Selected sequential playback,
project close/reopen, snapshot, and isolated restore were therefore not run.

- Built-in ImageGen: **4 of 4** (identity reference, opening still, preserved
  incompatible adaptation, adjoining still).
- New H3 I2V jobs: **2 of 2**, each submitted exactly once and ingested.
- No paid fallback, unknown-dispatch replay, gateway/configuration/source
  change, or cleanup of another task's staging occurred.

Step 4 remains partial. The next permitted action is to restore an attended
playback environment and perform genuine audiovisual review before any explicit
video selection or sequential/recovery exercise; the exhausted ImageGen and H3
pilot allowances do not authorize another generation or dispatch.
