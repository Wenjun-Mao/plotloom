# Deterministic F5A integration fixture

Portable test inputs derived from the retained F5 content-preserving split proof
and its matching F4 package. `storyboard.json` is byte-identical to the historical
`storyboard-split.json` with SHA-256
`50165863d45e678c28bcf794c6f9f5444b6aa45eef2caa446595dd638313578a`.
The matching accepted script, outline, cast, art and section map are included.
Source: “A keeper must power the beacon or dock before the storm closes the
channel.” Preserve one choice and two explicit endings; target 180 seconds.

Tests create disposable format-current projects through production HTTP APIs,
freeze new genuine current request packages and derive HTML through the pinned
upstream renderer. Completion provenance is explicitly `deterministic-fixture`;
it does not claim a new specialist run, human review, creative acceptance, media
or provider generation. Neither the rejected historical package nor retained
project databases are used or changed.
